import { HomeComponent } from './home.component';

export default {
  title: 'HomeComponent',
};

export const primary = () => ({
  moduleMetadata: {
    imports: [],
  },
  component: HomeComponent,
  props: {},
});
